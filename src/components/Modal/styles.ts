import styled from 'styled-components';

export const Button = styled.button`
  border: 0;
  border-radius: 0.5rem;
  padding: 0.625rem;
  display: flex;
  position: absolute;
  top: 20px;
  right: 20px;
  transition: 0.3s;

  &:hover {
    background: #e4e4e4;
  }
`;
